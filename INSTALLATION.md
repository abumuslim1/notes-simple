# Notes Service - Инструкция по установке и запуску

Notes Service — это полнофункциональное приложение для управления заметками с системой лицензирования, аутентификацией и поддержкой организации контента в папках.

## 📋 Требования

Перед установкой убедитесь, что на вашей системе установлены:

- **Node.js** версии 18.0 или выше
- **npm** или **pnpm** (рекомендуется pnpm 8.0+)
- **Git** для клонирования репозитория
- **Python 3.7+** для работы со скриптом генерации лицензионных ключей

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/abumuslim1/notes-simple.git
cd notes-simple
git checkout release
```

### 2. Установка зависимостей

```bash
pnpm install
```

### 3. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта с необходимыми переменными:

```env
# Database
DATABASE_URL=mysql://user:password@localhost:3306/notes_service

# Authentication
JWT_SECRET=your-secret-key-here
VITE_APP_ID=your-app-id
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://portal.manus.im

# Owner Information
OWNER_NAME=Your Name
OWNER_OPEN_ID=your-open-id

# Built-in APIs
BUILT_IN_FORGE_API_URL=https://api.manus.im
BUILT_IN_FORGE_API_KEY=your-api-key
VITE_FRONTEND_FORGE_API_URL=https://api.manus.im
VITE_FRONTEND_FORGE_API_KEY=your-frontend-key

# Analytics (optional)
VITE_ANALYTICS_ENDPOINT=https://analytics.example.com
VITE_ANALYTICS_WEBSITE_ID=your-website-id

# App Configuration
VITE_APP_TITLE=Notes Service
VITE_APP_LOGO=/logo.svg
```

### 4. Инициализация базы данных

```bash
pnpm db:push
```

Эта команда создаст все необходимые таблицы в базе данных.

### 5. Запуск приложения

```bash
pnpm dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## 🔐 Система лицензирования

Notes Service включает встроенную систему лицензирования с поддержкой пробного периода на 10 дней.

### Получение ID сервиса

1. После первого запуска приложения перейдите на страницу **Лицензии** (в боковом меню)
2. Скопируйте **Server ID** — это уникальный идентификатор вашего сервера

### Генерация лицензионного ключа

Используйте прилагаемый скрипт `license_generator.py` для создания лицензионных ключей:

```bash
python3 license_generator.py
```

Скрипт запустит интерактивное меню, где вам потребуется ввести:

- **Server ID** — скопированный с страницы Лицензии
- **Owner Name** — имя владельца лицензии (компания или ФИО)
- **License Duration** — количество дней, на которые выдается лицензия (по умолчанию 365)

**Пример использования:**

```
======================================================================
  Notes Service - License Key Generator
======================================================================

Main Menu
----------------------------------------------------------------------
1. Generate a new license key
2. Verify an existing license key
3. Exit

Select option (1-3): 1

Generate New License Key
----------------------------------------------------------------------
Enter Server ID (from License page): ABC123DEF456GHI789
Enter Owner Name: Acme Corporation
Enter License Duration (days) [365]: 365

⏳ Generating license key...

======================================================================
✅ License Key Generated Successfully!
======================================================================

📋 License Information:
  Server ID:     ABC123DEF456GHI789
  Owner Name:    Acme Corporation
  Valid Days:    365
  Expires At:    2025-12-05T08:30:45.123456Z

🔑 License Key (Copy and paste into License page)
----------------------------------------------------------------------
ABCD-EF12-GH34-IJ56-KL78-MN90-OP12-QR34-ST56-UV78-WX90-YZ12-AB34-CD56
----------------------------------------------------------------------

📝 Instructions:
  1. Copy the license key above (Ctrl+C / Cmd+C)
  2. Go to the License page in Notes Service
  3. Paste the key into the activation field
  4. Click 'Activate License'

Press Enter to continue...
```

### Активация лицензии

1. Скопируйте сгенерированный лицензионный ключ
2. Перейдите на страницу **Лицензии** в приложении
3. Вставьте ключ в поле **Лицензионный ключ**
4. Нажмите кнопку **Активировать лицензию**

После активации лицензия будет действительна указанное количество дней.

### Пробный период

Если лицензия не активирована, приложение работает в режиме пробного периода на **10 дней**. По истечении этого периода доступ будет заблокирован.

**Индикатор пробного периода** отображается в верхней части приложения:

- 🔵 **Синий** — пробный период активен (более 3 дней осталось)
- 🟠 **Оранжевый** — пробный период заканчивается (3 дня или менее)
- 🔴 **Красный** — пробный период истек, доступ заблокирован

## 📁 Структура проекта

```
notes-service/
├── client/                 # React приложение (фронтенд)
│   ├── src/
│   │   ├── pages/         # Страницы приложения
│   │   ├── components/    # Переиспользуемые компоненты
│   │   ├── lib/           # Утилиты и библиотеки
│   │   └── App.tsx        # Главный компонент приложения
│   └── public/            # Статические файлы
├── server/                # Express приложение (бэкенд)
│   ├── routers/           # tRPC маршруты
│   ├── db.ts              # Функции работы с БД
│   ├── license.ts         # Логика лицензирования
│   └── _core/             # Внутренние утилиты
├── drizzle/               # Миграции и схема БД
├── shared/                # Общий код для клиента и сервера
├── storage/               # Работа с S3 хранилищем
├── INSTALLATION.md        # Этот файл
├── LICENSE.md             # Лицензия проекта
└── package.json           # Зависимости проекта
```

## 🧪 Тестирование

Проект включает полный набор тестов, написанных на Vitest:

```bash
# Запуск всех тестов
pnpm test

# Запуск тестов в режиме наблюдения
pnpm test:watch

# Запуск тестов с покрытием
pnpm test:coverage
```

**Текущее покрытие тестами:**

- ✅ 1 тест аутентификации
- ✅ 11 тестов системы лицензирования
- ✅ 13 тестов управления заметками

Всего: **25 тестов**, все проходят успешно.

## 🏗️ Сборка для продакшена

```bash
pnpm build
```

Команда создаст оптимизированную сборку в директории `dist/`.

## 📚 Основные возможности

### Управление заметками

- ✅ Создание, редактирование и удаление заметок
- ✅ Организация заметок в папки
- ✅ Поиск по содержимому заметок
- ✅ Теги и избранные заметки
- ✅ Защита заметок паролем

### Аутентификация

- ✅ OAuth интеграция (Manus)
- ✅ Управление сессиями
- ✅ Роли пользователей (admin/user)

### Лицензирование

- ✅ Уникальный ID сервера
- ✅ Генерация лицензионных ключей
- ✅ Пробный период (10 дней)
- ✅ Проверка лицензии при каждом запросе
- ✅ Отображение статуса лицензии

## 🔧 Команды разработки

| Команда | Описание |
|---------|----------|
| `pnpm dev` | Запуск dev сервера с hot reload |
| `pnpm build` | Сборка для продакшена |
| `pnpm preview` | Предпросмотр production сборки |
| `pnpm test` | Запуск тестов |
| `pnpm test:watch` | Запуск тестов в режиме наблюдения |
| `pnpm db:push` | Применение миграций БД |
| `pnpm db:studio` | Открытие Drizzle Studio для управления БД |
| `pnpm lint` | Проверка кода на ошибки |

## 📖 Документация

- [API документация](./API.md) — описание всех API эндпоинтов
- [Архитектура](./ARCHITECTURE.md) — описание структуры проекта
- [Развертывание](./DEPLOYMENT.md) — инструкции по развертыванию на сервере

## 🐛 Решение проблем

### Ошибка подключения к БД

Убедитесь, что:
- Переменная `DATABASE_URL` установлена правильно
- База данных доступна и работает
- Учетные данные для подключения верны

### Лицензия не активируется

- Проверьте, что Server ID скопирован правильно
- Убедитесь, что ключ не истек
- Попробуйте скопировать ключ еще раз (без пробелов)

### Приложение не запускается

```bash
# Очистите кэш и переустановите зависимости
rm -rf node_modules pnpm-lock.yaml
pnpm install

# Пересоздайте базу данных
pnpm db:push
```

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте раздел [Решение проблем](#решение-проблем) выше
2. Посмотрите логи приложения для деталей ошибки
3. Откройте issue на GitHub репозитории

## 📄 Лицензия

Этот проект лицензирован под [MIT License](./LICENSE.md)

## 🎉 Начало работы

После установки и активации лицензии вы можете:

1. Создать первую заметку через кнопку "+" в боковом меню
2. Организовать заметки в папки
3. Использовать поиск для быстрого поиска заметок
4. Добавлять теги и отмечать избранные заметки
5. Делиться заметками с другими пользователями (если включена функция)

Спасибо за использование Notes Service! 🚀
