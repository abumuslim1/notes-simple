# Notes Service 📝

Полнофункциональный сервис для создания и управления заметками с поддержкой авторизации, папок, тегов, защиты паролем и прикрепления файлов.

## ✨ Возможности

- 🔐 **Авторизация** - OAuth интеграция с управлением ролями (admin/user)
- 📝 **Редактор заметок** - Rich text редактор TipTap с форматированием
- 📁 **Система папок** - Организация заметок по папкам
- 🏷️ **Теги** - Добавление и фильтрация по тегам
- 🔒 **Защита паролем** - Защита отдельных заметок паролем
- 📎 **Вложения** - Прикрепление файлов до 50МБ
- 📜 **История версий** - Просмотр всех версий заметки
- ⭐ **Избранное** - Быстрый доступ к важным заметкам
- 🔍 **Поиск** - Полнотекстовый поиск по заметкам
- 🔐 **Генератор паролей** - Встроенный инструмент для генерации паролей
- 👥 **Управление пользователями** - Создание, редактирование, удаление пользователей (только для админов)
- 🎨 **Минималистичный дизайн** - Светлая тема с удобным интерфейсом

## 🚀 Быстрый старт

### Автоматическая установка (рекомендуется)

```bash
curl -fsSL https://raw.githubusercontent.com/abumuslim1/notes-simple/main/install.sh | sudo bash
```

Скрипт автоматически:
- Установит все необходимые зависимости (Node.js, pnpm, MySQL)
- Клонирует репозиторий
- Создаст и настроит базу данных
- Установит зависимости приложения
- Соберет приложение
- Запустит приложение на `http://localhost:3000`

### Ручная установка

Подробная инструкция доступна в файле [DEPLOYMENT.md](./DEPLOYMENT.md)

## 📋 Требования

- Node.js 18+
- npm или pnpm
- MySQL 8.0+
- Git

## 🛠️ Разработка

### Установка зависимостей

```bash
pnpm install
```

### Запуск в режиме разработки

```bash
pnpm dev
```

Приложение будет доступно на `http://localhost:3000`

### Сборка для продакшена

```bash
pnpm build
pnpm start
```

### Запуск тестов

```bash
pnpm test
```

## 📁 Структура проекта

```
notes-service/
├── client/                 # Frontend (React + Vite)
│   ├── src/
│   │   ├── pages/         # Страницы приложения
│   │   ├── components/    # React компоненты
│   │   ├── lib/           # Утилиты и библиотеки
│   │   └── index.css      # Глобальные стили
│   └── index.html
├── server/                 # Backend (Express + tRPC)
│   ├── routers.ts         # API endpoints
│   ├── db.ts              # Database queries
│   └── _core/             # Core framework
├── drizzle/               # Database schema
│   └── schema.ts          # Таблицы БД
├── storage/               # S3 storage helpers
├── shared/                # Общие типы и константы
├── install.sh             # Скрипт автоматической установки
├── DEPLOYMENT.md          # Инструкция по развертыванию
└── package.json
```

## 🔧 Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Database
DATABASE_URL="mysql://user:password@localhost:3306/notes_service"

# JWT Secret (минимум 32 символа)
JWT_SECRET="your_jwt_secret_here_min_32_chars"

# OAuth (опционально)
VITE_APP_ID="your_app_id"
OAUTH_SERVER_URL="https://api.manus.im"
VITE_OAUTH_PORTAL_URL="https://manus.im/login"

# App Configuration
VITE_APP_TITLE="Notes Service"
OWNER_NAME="Admin"
OWNER_OPEN_ID="admin_user_id"
```

## 📚 Документация

- [Инструкция по развертыванию](./DEPLOYMENT.md) - Подробная инструкция по установке на сервер
- [API документация](./API.md) - Описание всех API endpoints

## 🐛 Решение проблем

### Ошибка подключения к базе данных

Убедитесь, что:
1. MySQL запущен: `sudo systemctl status mysql`
2. Учетные данные в `.env` верны
3. База данных создана: `mysql -u root -p -e "SHOW DATABASES;"`

### Ошибка портов

Если порт 3000 занят:
```bash
PORT=3001 pnpm dev
```

### Очистка кеша

```bash
rm -rf node_modules pnpm-lock.yaml
pnpm install
pnpm build
```

## 📦 Развертывание

### Docker

```bash
docker build -t notes-service .
docker run -p 3000:3000 \
  -e DATABASE_URL="mysql://user:password@db:3306/notes_service" \
  notes-service
```

### PM2

```bash
npm install -g pm2
pnpm build
pm2 start dist/index.js --name "notes-service"
pm2 save
pm2 startup
```

### Nginx (reverse proxy)

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 🤝 Вклад

Приветствуются pull requests! Для больших изменений сначала откройте issue для обсуждения.

## 📄 Лицензия

MIT

## 📞 Поддержка

Если у вас возникли проблемы, создайте issue на GitHub:
https://github.com/abumuslim1/notes-simple/issues

## 🎯 Планы развития

- [ ] Синхронизация в реальном времени (WebSocket)
- [ ] Совместное редактирование
- [ ] Экспорт в PDF/Markdown
- [ ] Мобильное приложение
- [ ] Синхронизация между устройствами
- [ ] Поддержка Markdown
- [ ] Темы оформления

---

**Создано с ❤️ для управления вашими идеями**
