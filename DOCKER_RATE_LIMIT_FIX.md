# 🔧 Решение проблемы Docker Hub Rate Limit

## ❌ Проблема

При установке вы получаете ошибку:

```
toomanyrequests: You have reached your unauthenticated pull rate limit.
https://www.docker.com/increase-rate-limit
```

## 💡 Причина

Docker Hub ограничивает количество запросов для неавторизованных пользователей:
- **Анонимные пользователи:** 100 запросов за 6 часов с одного IP
- **Авторизованные пользователи:** 200 запросов за 6 часов

## ✅ Решения

### Решение 1: Использовать обновленный скрипт (Рекомендуется)

Обновленный скрипт `deploy-timeweb.sh` уже содержит исправления:

```bash
# Скачайте последнюю версию
curl -O https://raw.githubusercontent.com/abumuslim1/notes-simple/release/deploy-timeweb.sh
chmod +x deploy-timeweb.sh
sudo ./deploy-timeweb.sh
```

**Что делает новый скрипт:**
- ✅ Настраивает зеркала Docker registry
- ✅ Ограничивает параллельные загрузки
- ✅ Предварительно загружает образ node:20-alpine
- ✅ Повторяет попытки при ошибках
- ✅ Удаляет устаревшее поле `version` из docker-compose.yml

### Решение 2: Ручная настройка Docker

Если вы уже начали установку, выполните:

```bash
# 1. Создайте конфигурацию Docker
sudo mkdir -p /etc/docker
sudo cat > /etc/docker/daemon.json << 'EOF'
{
  "registry-mirrors": [
    "https://mirror.gcr.io"
  ],
  "max-concurrent-downloads": 3,
  "max-concurrent-uploads": 3
}
EOF

# 2. Перезапустите Docker
sudo systemctl restart docker

# 3. Подождите несколько секунд
sleep 5

# 4. Попробуйте загрузить образ вручную
docker pull node:20-alpine

# 5. Запустите приложение
cd /opt/notes-service
docker-compose up -d --build
```

### Решение 3: Авторизация в Docker Hub

Создайте бесплатный аккаунт на Docker Hub:

```bash
# 1. Зарегистрируйтесь на https://hub.docker.com/

# 2. Авторизуйтесь на сервере
docker login

# 3. Введите логин и пароль

# 4. Запустите приложение
cd /opt/notes-service
docker-compose up -d --build
```

### Решение 4: Подождать

Если вы превысили лимит, подождите 6 часов и попробуйте снова.

Или используйте VPN/прокси для смены IP адреса.

## 🔍 Проверка статуса

### Проверить текущий лимит:

```bash
TOKEN=$(curl "https://auth.docker.io/token?service=registry.docker.io&scope=repository:ratelimitpreview/test:pull" | jq -r .token)
curl --head -H "Authorization: Bearer $TOKEN" https://registry-1.docker.io/v2/ratelimitpreview/test/manifests/latest
```

Смотрите заголовки:
- `ratelimit-limit` - Общий лимит
- `ratelimit-remaining` - Осталось запросов

### Проверить конфигурацию Docker:

```bash
# Просмотр конфигурации
cat /etc/docker/daemon.json

# Проверка статуса Docker
systemctl status docker

# Просмотр информации о Docker
docker info | grep -i mirror
```

## 📝 Обновленные файлы

В новой версии исправлены:

1. **deploy-timeweb.sh**
   - Добавлена настройка зеркал Docker
   - Добавлена предварительная загрузка образа
   - Добавлены повторные попытки при ошибках

2. **docker-compose.yml**
   - Удалено устаревшее поле `version: '3.8'`

## 🚀 Быстрый старт с исправлениями

```bash
# На сервере Timeweb:
ssh root@your-server-ip

# Скачайте обновленный скрипт
curl -O https://raw.githubusercontent.com/abumuslim1/notes-simple/release/deploy-timeweb.sh
chmod +x deploy-timeweb.sh

# Запустите установку
sudo ./deploy-timeweb.sh
```

## 💡 Дополнительные советы

### Если проблема повторяется:

1. **Очистите Docker кэш:**
   ```bash
   docker system prune -a
   ```

2. **Используйте альтернативные зеркала:**
   ```json
   {
     "registry-mirrors": [
       "https://mirror.gcr.io",
       "https://docker.mirrors.ustc.edu.cn",
       "https://registry.docker-cn.com"
     ]
   }
   ```

3. **Проверьте интернет соединение:**
   ```bash
   ping -c 4 registry-1.docker.io
   ```

## 📞 Поддержка

Если проблема не решается:

- **GitHub Issues:** https://github.com/abumuslim1/notes-simple/issues
- **Документация:** INSTALL_TIMEWEB.md

## ✅ Готово!

После применения любого из решений, установка должна пройти успешно.

**Удачи!** 🎉
